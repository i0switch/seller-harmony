# テスト20: レスポンシブ・モバイルUI

> **カテゴリ**: レスポンシブデザイン  
> **優先度**: P1 (High)  
> **推定所要時間**: 25分  
> **前提条件**: 各ロールでログイン可能

---

## AIエージェントへの指示

```
全ページのレスポンシブ・モバイルUI対応を検証してください。
テスト環境: https://preview--member-bridge-flow.lovable.app/

以下の画面幅で各ページを確認:
  - デスクトップ: 1280px以上
  - タブレット: 768px
  - モバイル: 375px (iPhone SE), 390px (iPhone 14)

ブラウザのDevToolsでビューポートサイズを変更して確認してください。
テーブル→カード切り替え、サイドバーの折りたたみ、フォームの幅、
テキストの切り詰め/折り返し、タッチターゲットのサイズを重点的に確認。
```

---

## TC-20-01: ランディングページのレスポンシブ

**手順**:
1. `/` を各画面幅で確認

**期待結果**:
- [ ] デスクトップ: 3つのカードが横並び
- [ ] タブレット: 2列 or 積み重ね表示
- [ ] モバイル: 1列の縦積み表示
- [ ] テキストが切れない
- [ ] カードが適切にパディング・マージンを持つ

---

## TC-20-02: Sellerサイドバーのモバイル対応

**手順**:
1. `/seller/dashboard` をモバイル幅(375px)で確認

**期待結果**:
- [ ] サイドバーがハンバーガーメニューまたはオーバーレイに変わる
- [ ] メニューの開閉が正常に動作する
- [ ] メニュー選択後にメニューが自動で閉じる
- [ ] コンテンツエリアが全幅を使用する

---

## TC-20-03: Platformサイドバーのモバイル対応

**手順**:
1. `/platform/dashboard` をモバイル幅で確認

**期待結果**:
- [ ] TC-20-02 と同様のサイドバー挙動

---

## TC-20-04: 会員テーブルのモバイルカード表示

**手順**:
1. `/seller/members` をモバイル幅で確認

**期待結果**:
- [ ] テーブルがカード形式に切り替わる
- [ ] 各カードに以下が表示:
  - 名前
  - Discordユーザー名 or メール
  - 課金ステータスバッジ
  - プラン名
  - ロールステータスバッジ
  - エラー表示（あれば）
- [ ] カードが縦に積み重なる
- [ ] スクロールが正常

---

## TC-20-05: テナント管理テーブルのモバイル対応

**手順**:
1. `/platform/tenants` をモバイル幅で確認

**期待結果**:
- [ ] テーブルがモバイルフレンドリーな表示に変わる
- [ ] 横スクロールまたはカード形式
- [ ] データの読みやすさが保たれる

---

## TC-20-06: Webhook監視テーブルのモバイル対応

**手順**:
1. `/platform/webhooks` をモバイル幅で確認

**期待結果**:
- [ ] テーブルデータが読みやすく表示される
- [ ] アクションボタンがタップ可能なサイズ

---

## TC-20-07: フォームのモバイル対応

**手順**:
1. `/seller/plans/new` をモバイル幅で確認
2. `/seller/signup` をモバイル幅で確認
3. `/platform/login` をモバイル幅で確認

**期待結果**:
- [ ] 入力フィールドが全幅に広がる
- [ ] ラベルが入力欄の上に配置（横並びにならない）
- [ ] ボタンがタップしやすいサイズ（44px以上の高さ推奨）
- [ ] キーボードが表示されてもフォームが隠れない

---

## TC-20-08: ダイアログ/モーダルのモバイル対応

**手順**:
1. モバイル幅でConfirmDialog/AlertDialogを開く（例: プラン停止）
2. モバイル幅で詳細ダイアログを開く（例: Webhook詳細）

**期待結果**:
- [ ] ダイアログがモバイル画面内に収まる
- [ ] スクロール可能（長いコンテンツの場合）
- [ ] 閉じるボタンがタップしやすい
- [ ] 背景オーバーレイをタップで閉じられる

---

## TC-20-09: FilterBarのモバイル対応

**手順**:
1. フィルター付きページをモバイル幅で確認

**期待結果**:
- [ ] 検索バーが全幅に広がる
- [ ] フィルターセレクトが適切なサイズ（`w-full sm:w-40` → モバイルでは全幅）
- [ ] 検索バーとフィルターが縦積み

---

## TC-20-10: ページネーションのモバイル対応

**手順**:
1. ページネーション付きページをモバイル幅で確認

**期待結果**:
- [ ] ページネーションボタンがタップ可能なサイズ
- [ ] ページ番号と件数表示が画面に収まる

---

## TC-20-11: KPIカードのモバイル対応

**手順**:
1. `/seller/dashboard` と `/platform/dashboard` をモバイル幅で確認

**期待結果**:
- [ ] KPIカードが1列または2列で表示
- [ ] 数値が切れない
- [ ] カード内のテキストが折り返される

---

## TC-20-12: Buyerページのモバイル対応

**手順**:
1. `/checkout/success`、`/buyer/discord/confirm`、`/member/me` をモバイル幅で確認

**期待結果**:
- [ ] 中央揃えレイアウトがモバイルでも有効
- [ ] ボタンが全幅でタップしやすい
- [ ] プランカードが適切に表示
- [ ] Discord連携説明のテキストが折り返される

---

## TC-20-13: 画面回転（横向き→縦向き）

**手順**:
1. モバイルビューで画面を横向き (landscape) に変更
2. 縦向き (portrait) に戻す

**期待結果**:
- [ ] レイアウトが自動的に再計算される
- [ ] UI要素が正しく再配置される
- [ ] スクロール位置が大きくずれない

---

## テスト完了チェックリスト

| TC | テスト名 | 結果 | 備考 |
|---|---|---|---|
| TC-20-01 | ランディング | | |
| TC-20-02 | Sellerサイドバー | | |
| TC-20-03 | Platformサイドバー | | |
| TC-20-04 | 会員テーブル | | |
| TC-20-05 | テナントテーブル | | |
| TC-20-06 | Webhookテーブル | | |
| TC-20-07 | フォーム | | |
| TC-20-08 | ダイアログ | | |
| TC-20-09 | FilterBar | | |
| TC-20-10 | ページネーション | | |
| TC-20-11 | KPIカード | | |
| TC-20-12 | Buyerページ | | |
| TC-20-13 | 画面回転 | | |
