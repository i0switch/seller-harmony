PROJECT_NAME="Seller Harmony Backend"
API_V1_STR="/api"

# Ensure to surround lists with brackets or format them correctly if pydantic expects JSON-like strings
# For now the default handles it internally in config.py, but you can override here.
# FRONTEND_CORS_ORIGINS='["http://localhost:8080", "http://localhost:5173"]'

SUPABASE_URL="https://your-project.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"

STRIPE_API_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."

DISCORD_BOT_TOKEN="your-bot-token"
